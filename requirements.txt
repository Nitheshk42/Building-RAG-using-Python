# Core
streamlit
python-dotenv
bcrypt
numpy<2.0.0
matplotlib
scikit-learn

# LangChain Core
langchain
langchain-community
langchain-core
langchain-text-splitters
langchain-huggingface

# Embeddings
sentence-transformers

# Vector Store
# Pinned to a pre-Rust-binding release: newer chromadb (0.5+) added a multi-tenant
# "admin database" layer (chromadb.Client -> _validate_tenant_database -> get_tenant)
# backed by Rust bindings that are unreliable on very new Python versions like 3.14 -
# exactly the failure this project kept hitting. 0.4.24 uses the older, simpler local
# persistence path and never invokes that tenant-validation code at all.
# NOTE: chromadb 0.4.24 still uses removed NumPy aliases (np.float_, np.uint) internally,
# which is why numpy is pinned to <2.0.0 above - NumPy 2.0 deleted those aliases.
chromadb==0.4.24
pysqlite3-binary

# PDF Handling
pypdf

# Word Handling
docx2txt
python-docx

# LLM (for Day 2)
langchain-groq

# Optional but useful
tiktoken